import { component } from 'riot';
import <%= props.name %> from './<%= props.tag %>.riot';

document.addEventListener('DOMContentLoaded', () => {
  component(<%= props.name %>)(document.querySelector('<%= props.tag %>') || document.body);
});
